apiVersion: v1
kind: Service
metadata:
  name: cashflow
  namespace: cashflow-staging
  labels:
    app.kubernetes.io/name: cashflow
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cashflow
  ports:
    - name: http
      port: 3000
      targetPort: http
