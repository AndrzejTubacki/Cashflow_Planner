apiVersion: v1
kind: Secret
metadata:
  name: cashflow-secrets
  namespace: cashflow-staging
  labels:
    app.kubernetes.io/name: cashflow
type: Opaque
stringData: {}

# Add only the keys your deployment uses. Do not commit real values.
# Example keys:
# CASHFLOW_PASSWORD_PEPPER:
# CASHFLOW_EXTERNAL_AUTH_SECRET:
# CASHFLOW_GOOGLE_CLIENT_SECRET:
# CASHFLOW_GITHUB_CLIENT_SECRET:
# CASHFLOW_FACEBOOK_CLIENT_SECRET:
